# Демо-файлы для `flask seed-db`

Положите документы в **`files/`**. При запуске `flask seed-db` для каждого файла создаётся распоряжение с вложением (нужен S3, как в продакшене).

Повторный импорт демо-распоряжений: `flask seed-db --force`
